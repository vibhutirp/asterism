# Superseded designs — do not implement

The user confirmed that the frontend only browses topics, memories, and sources. Memory creation happens through Slack and connected services. The web has neither Capture nor Ask.

All files in this folder describe earlier scope and are retained as history, not active specifications. Capture/Ask controls on the interaction sheet and old topic inspector are obsolete. The active authority is ../README.md and ../exploration-only.png.
